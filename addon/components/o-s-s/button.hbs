<button class={{this.computedClass}} {{did-insert this.didInsert}} ...attributes>
  {{#if this.loadingState}}
    <i class="fas fa-circle-notch fa-spin"></i>
  {{else}}
    {{yield}}
  {{/if}}
</button>
